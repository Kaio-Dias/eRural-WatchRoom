# Configuração para deploy Docker no Heroku
build:
  docker:
    web: Dockerfile.heroku

run:
  web: bundle exec puma -C config/puma.rb
  
# Release phase desabilitada para deploy mais rápido
# Rode as migrations manualmente com:
# heroku run bin/rails db:migrate -a watch-staging
# release:
#   image: web
#   command:
#     - bin/rails db:migrate
